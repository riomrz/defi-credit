NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
NEXT_PUBLIC_IOTA_NETWORK=testnet

# Set this after deploying Move contracts with `iota client publish`
# Example: NEXT_PUBLIC_MOVE_PACKAGE_ID=0xabc123...
# Leave empty to run in simulation mode (off-chain only)
NEXT_PUBLIC_MOVE_PACKAGE_ID=
